import React from 'react';
import PropTypes from 'prop-types';
import { selectTotalQquant } from './selectors';

const CartFeature = (props) => {
   return <div>Cart Page</div>;
};

CartFeature.propTypes = {};

export default CartFeature;
